#include<iostream>

using namespace std;

int main(){
    int n,m,k;
    while(cin>>n>>m>>k && n!=0){
        int x[k],y[k],t[k];
        int xi,yi;
        for(int i=0;i<k;i++){
            cin>>x[i]>>y[i]>>t[i];
        }
        cin>>xi>>yi;
    }
}